# Agent Instructions

## Combine
- Do not attempt to run `combine.sh` after each change

## Git & Commits
- NEVER perform `git commit` or `git push` unless explicitly asked by the user.
- You may use `git add` to stage files for the user if it helps their workflow, but always stop before committing.
- Assume the user wants to review and commit changes themselves.
