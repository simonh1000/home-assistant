alias: "Notify Simon"
fields:
  title:
    example: "EV Paused"
  message:
    example: "Something happened"
sequence:
  - action: notify.mobile_app_pixel_10
    data:
      title: "{{ title }}"
      message: "{{ message }}"
      data: { priority: high, ttl: 0, importance: high }
mode: parallel
